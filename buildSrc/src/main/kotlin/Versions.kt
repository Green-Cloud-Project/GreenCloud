object Versions {

    // Class Path for Plugin
    const val buildGradleVersion = "4.1.3"
    const val googleServiceVersion = "4.3.5"
    const val sentryGradleVersion = "1.7.36"
    const val r8Version = "2.1.75"
    const val androidJunit5Version = "1.6.2.0"
    const val androidMavenGradleVersion = "2.1"
    const val detektGradleVersion = "1.15.0"
    const val navSafArgPluginVersion = "2.3.3"

    // Kotlin
    const val kotlinVersion = "1.4.32"

    // Coroutines
    const val coroutineVersion = "1.4.3"

    // RxJava
    const val rxJavaVersion = "2.2.21"
    const val rxAndroidVersion = "2.1.1"

    const val junitVersion = "4.13.2"

    // AndroidX
    const val appCompatVersion = "1.2.0"
    const val constraintLayoutVersion = "2.0.4"
    const val coreKtxVersion = "1.3.2"
    const val legacySupportVersion = "1.0.0"
    const val activityVersion = "1.2.2"
    const val fragmentVersion = "1.3.2"
    const val recyclerviewVersion = "1.1.0"
    const val viewpager2Version = "1.0.0"
    const val databindingVersion = "4.1.3"
    const val desugarJdkVersion = "1.1.1"

    // Jetpack
    const val lifeCycleVersion = "2.2.0"
    const val roomVersion = "2.2.6"
    const val navigationVersion = "2.3.4"
    const val startupVersion = "1.0.0"

    const val testVersion = "1.3.0"
    const val espressoVersion = "3.3.0"
    const val junitExtVersion = "1.1.2"

    // Google
    const val materialVersion = "1.3.0"
    const val gsonVersion = "2.8.6"
    const val truthVersion = "1.1.2"

    // Square
    const val retrofitVersion = "2.9.0"
    const val okhttp3Version = "4.9.1"
    const val leakcanaryVersion = "2.6"

    // Aws Sdk
    const val awsSdkVersion = "2.19.0"

    // Debug
    const val timberVersion = "4.7.1"
    const val stethoVersion = "1.5.1"
    const val sentryVersion = "4.3.0"

    // File Downloader
    const val fetch2Version = "3.1.6"

    // Recyclerview Delegate
    const val adapterdelegatesVersion = "4.3.0"

    // Klint
    const val klintVersion = "0.41.0"

    // Koin
    const val koinVersion = "2.2.2"

    // Mockk
    const val mockkVersion = "1.10.6"

    // Mockito
    const val mockitoVersion = "3.7.7"

    // Speck2
    const val junitPlatformVersion = "1.7.0"
    const val spek2Version = "2.0.15"

    // Flipper
    const val flipperVersion = "0.74.0"
    const val soloaderVersion = "0.9.0"

    // Coil
    const val coilVersion = "1.1.1"

    // Glide
    const val glideVersion = "4.12.0"

    // Play Service
    const val playService = "17.0.0"

    // Rx..
    const val rxLocationVersion = "1.0.5"
    const val rxPermissionVersion = "0.10.2"

    // zwing
    const val zxing ="3.6.0"

    // Social Loging
    const val kakao = "1.21.1"
    const val naver = "4.2.5"
    const val facebook = "5.9.0"

    // Map util
    const val mapUtilVersion = "0.6.2"
}